
<div class="container">
    <h1 class="heading-1">Top Chefs</h1>
</div>

<div class="my-4 d-flex overflow-x">
  <% chefs.each do | chef | %>
      <div class="container">
        <div class="card-category card-home" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path chef.user.photo.key%>)"><%= link_to chef.user.first_name, chef_path(chefs),class: "text-center recipe-link" %>
        </div>
        <div class="recipe-info card-body pb-0">
              <%# <% chefs.chef_reviews.each do |review| %>
               <%# <em><%= review.content %>
        </div>
      </div>
    <%# <% end %> 
  <% end %>
  
</div>

